// import { useContext } from "react";
import Navbar from "./partials/Navbar";

const Home = () => {
  // const {count} = useContext(UserContext);
  return (
    <>
    <Navbar/>
      <p>Home page</p>
    </>
  );
};

export default Home;
